<h1>レビュー投稿</h1>
<%= form_with model: @post do |f| %>
  
  <p>ユーザーネーム：<%= current_user.user.name %></p>
  <h4>サブスク名</h4>


  <h4>タイトル</h4>
  <%= f.text_area :title %>
  #投稿画像
  <%= f.file_field :image, accept: "image/*" %>
  <h4>本文</h4>
  <%= f.text_area :body %>
  <%= f.submit '投稿' %>

<% end %>
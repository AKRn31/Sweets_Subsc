<h1>レビュー投稿</h1>
<%= form_with model: @post do |f| %>
  
  <p>ユーザーネーム<%= @user.username %></p>
  <p>サブスク名<%= f.text_area :name %></p>
  <p>タイトル:<%= f.text_area :title %></p>
  #投稿画像
  <%= f.file_field :image, accept: "image/*" %>
  <p>詳細 <%= f.text_area :body %></p>
  <%= f.submit '投稿' %>

<% end %>